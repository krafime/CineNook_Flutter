name: cinenook
description: "A new Flutter project."

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  back_button_interceptor: ^8.0.4
  # bloc: ^9.0.0  # Removing BLoC packages
  cached_network_image: ^3.3.1
  carousel_slider: ^5.0.0
  country_codes: ^3.1.0+2
  cupertino_icons: ^1.0.8
  equatable: ^2.0.5
  firebase_auth: ^5.5.1
  firebase_core: ^3.12.1
  flutter:
    sdk: flutter
  # flutter_bloc: ^9.1.0  # Removing BLoC packages
  flutter_glow: ^0.3.2
  flutter_staggered_grid_view: ^0.7.0
  get: ^4.6.6 # Adding GetX package
  go_router: ^14.8.1
  google_fonts: ^6.2.1
  google_sign_in: ^6.3.0
  http: ^1.2.2
  intl: ^0.20.2
  logging: ^1.3.0
  visibility_detector: ^0.4.0+2

dev_dependencies:
  change_app_package_name: ^1.3.0
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
